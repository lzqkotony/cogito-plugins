# 开发工作记录

按时间线记录这个项目做过什么、谁在什么时候定了什么决策。**换电脑接手时，先看这份 + `design.md`**。

---

## 2026-09-15 · 第一台机器（Windows / seewo）

- 搭 Java 开发环境（JDK 21 与 26、Maven 3.9.16、Gradle 9.7.1，装在本地 `tools/`）
- 确定方向：做一个「脑叶」主题的 Paper 服务端插件（原型 Project Moon 三部曲）
- 实现 **v0.1**：
  - 脑啡肽做成**实体物品**（NBT 标签的附魔绿宝石块）
  - 接 **Vault 经济**：100 金钱 → 1 脑啡肽，**单向不可逆**
  - `/enkephalin`（查看/发放/扣除/查看配置/重载）、Tab 补全、权限节点
  - 全部文案走 MiniMessage，配置与日志 UTF-8
- 开源：GitHub `lzqkotony/cogito-plugins`，**GPL-3.0**，发布 **0.1.0-BETA** Release（jar + sha256）
- 导出交接包（聊天记录 + 工程 + 脚本），放到桌面，交给另一台电脑

## 2026-09-15 晚 · 第二台机器（Windows / Administrator）

- 读交接包 → 先搭**离线开发环境包**（JDK + Maven + Git + 本地仓库快照，约 324 MB）
- 新增**箱子 GUI**：`/cogito gui`（别名 `/cog`、`/box`，`/enkephalin box` 同效）
  - 5 行 45 格：持有量信息、余额、×1/×8/×64 兑换按钮、命令帮助、管理员配置/重载、关闭
  - 菜单物品锁死（点击/拖拽全部取消），不会被 shift 搬走
- 把两份聊天记录与手写设计稿原图放进 `chat/`，README 标注为 vibe coding 项目
- 推送 GitHub（`39abe29`、`930dc39`）

## 2026-09-16 · 第一台机器（回到本机，先 clone 再继续）

- 从 GitHub 克隆/快进到最新，读第二台机器的聊天记录，确认 GUI 改动
- 按**第二批手写稿**（08:08 拍的三页）实现**物品注册体系（v0.2.0-BETA）**：
  - `items.yml` 注册 5 个物品：`pe` 脑啡肽(绿宝石)、`pe-module` 脑啡肽模块(绿宝石块，别名 PE-BOX)、`cogito` Cogito(跳跃药水，堆叠上限改成 64)、`ego-blueprint` E.G.O 研发图纸(纸)、`golden-bough` 金枝(树枝)
  - 统一 NBT 标签 `cogito:item=<id>`；`legacy-tags` 兼容旧 `enkephalin` 标签，老物品不失效
  - **原版行为拦截**：注册物品不可放置、不可参与合成（可按物品配置）
  - 新命令：`/cogito items`、`/cogito give <物品id> <玩家> <数量>`、`/cogito debug <物品id>`（自检）
  - `config.yml`（经济/界面）与 `items.yml`（物品）分家
- **验证**（都是真跑出来的）：
  - Maven 编译 14 个源文件通过，产物 `Cogito-0.2.0-BETA.jar`
  - 本地 Paper 测试服**后台无窗口**启动，日志：`已注册 5 个物品：pe, pe-module, cogito, ego-blueprint, golden-bough`、`Vault 经济已连接：EssentialsX Economy`
  - `/cogito debug pe` → 材质 EMERALD、堆叠 64、标签 `cogito:item=pe`、识别通过
  - `/cogito debug cogito` → POTION、**堆叠上限 64**、识别通过
- 第二批设计稿整理进 `docs/design.md`（含命令清单与实现状态、物品表、E.G.O 抗性公式），三张手写稿原图入库
- 推送 GitHub（`60f6948`）

## 2026-09-16 · 第一台机器（玩家数据层 core data）

- 实现 **SQLite 玩家数据层**（`data/` 包）：`PlayerProfile`（等级 / 镇压次数 / E.G.O 解锁）、
  `SqlitePlayerDataStore`（三张表、WAL、事务 upsert）、`PlayerDataService`（内存缓存 + 每 30 秒异步落库 + 关服写回）
  - 驱动直接用服务端自带的 `org.xerial:sqlite-jdbc`，不额外引入依赖、不 shade
- 新命令：`/cogito set Lv <玩家> <数字>`、`/cogito reset player_information <玩家>`、
  `/cogito data <玩家>`、`/cogito debug db`（数据库读写自检）
- 版本 → **0.3.0-BETA**
- 用 **Docker** 起了个 Paper 1.21.11 实例（WSL Arch + itzg/minecraft-server 镜像）实际验证：
  插件加载、启用、物品注册、数据库初始化、Vault 连接全部正常，无异常
  （中途踩坑：Docker Hub 被墙 → 配国内镜像源；镜像自身要从 raw.githubusercontent.com 拉默认配置会卡住，与插件无关）
- 918（九一八纪念）插件的草稿写完但**未采用**，用户交给另一位开发者，草稿移到 `work/other-projects/mingji-918-draft/`

## 2026-09-17 · 新机器（Linux）· E.G.O. 伤害与抗性

- 从 GitHub 重新 clone，读取此前全部工作记录，确认接手点
- 明确 E.G.O. 伤害规则（用户拍板）：
  - 攻击侧只有 **红伤 / 蓝伤** 两条通道
  - **蓝伤只能由 E.G.O. 来源造成**
  - 防具侧每套 E.G.O. 只有一个统一抗性 `x`，对**所有传入伤害**生效，不按伤害类型区分
  - `x` 不设代码范围，直接使用 `ego.yml` 中标注的值
  - `y = 同一套有效 E.G.O. 防具件数`，不设置部位权重；武器与饰品不计入
  - 公式 `r = 1 - (1 - x) * y / 4`，最终伤害保持 `double` 交给 Minecraft
  - 混入原版防具或其他 E.G.O. 防具时，全部 E.G.O. 抗性失效，`r=1`
  - E.G.O. 防具不能附魔，不提供原版护甲值与盔甲韧性
- 实现 `ego/` 包：伤害通道、装备部位、套装注册、纯公式、伤害服务与监听器
- 新增 `ego.yml`，内置可测试的 `失乐园` 套装：`x=0.1`、四件防具、红/蓝测试武器、饰品
- 新增管理员自检：
  - `/cogito give ego <玩家> <套装id> <数量>` 发放整套 E.G.O.
  - `/cogito debug ego <玩家>` 查看当前套装、件数 `y`、抗性 `x` 与倍率 `r`
  - `/cogito debug attack <red|blue> <攻击者> <目标> <伤害>` 验证蓝伤来源限制
- 同步修复：**Cogito 不能被饮用**，右键不再触发跳跃药水效果
- 版本 → **0.4.0-BETA**，增加 Maven 单元测试与 GitHub Actions 自动构建 / Release
- 本地 `mvn clean test package`：BUILD SUCCESS，5/5 测试通过；JAR SHA-256 `e9b7e9c3a75365937a45d76068179e17d0472ee9ee8cc9d842a4e2607900084f`
- 正式服部署：停止实例 → 备份并停用 `Cogito-0.3.0-BETA.jar` → 写入 `Cogito-0.4.0-BETA.jar` → 启动实例
- 正式服加载验证：识别 `Cogito v0.4.0-BETA`、注册 5 个基础物品和 1 套 E.G.O.（7 件）、SQLite 与 Vault 正常
- 控制台自检：`/cogito items` 返回 12 个注册物品；`/cogito debug cogito` 识别到 `cogito:item=cogito`、堆叠上限 64、不可放置/合成

## 2026-09-17 · 第一台机器（E.G.O. 开发台与专属图纸）

- 设计稿（09-17 手写页）确认：开发台方块、专属图纸解锁研发、爆炸抗性同黑曜石、需钻石镐以上
- 用户拍板：**图纸不是消耗品**，每套 E.G.O. 有自己专属的一张，右键解锁后仍留在手上；
  研发规则用新的（解锁后每次研发只消耗脑啡肽）；代价与共鸣后面再说
  > ⚠️ **已被后续设计取代**：09-18 手写稿把蓝图改成了**一次性装载物**（装载时消耗 1 张，研发能力绑定玩家），
  > 0.5.1 起按此实现。用户 2026-09-21 明确「**以当前开发进度为准**」，故本条"不消耗"的说法作废。
- 实现：
  - `develop/DevelopTableManager`：开发台坐标记进**区块 PDC** + 上方 `TextDisplay` 全息文本
  - `listener/DevelopListener`：放置登记、钻石镐限制、爆炸保护、右键开界面、图纸右键解锁
  - `gui/DevelopMenu`：列出套装（抗性 / 消耗 / 是否解锁），点击消耗脑啡肽产出整套
  - 合成表：四角下界合金块 / 四边脑啡肽模块 / 中心工作台（`ShapedRecipe`，重载时自动替换）
  - 图纸物品由 `EgoRegistry` 按 `blueprint-<套装id>` 自动生成；`ego.yml` 新增 `develop.cost`
  - 修正 `ItemBehaviourListener`：插件自己的配方允许使用自定义物品当材料
- 版本 → **0.5.0-BETA**；`mvn clean test package` 通过（31 源文件、5/5 测试）

## 2026-09-19 · 0.5.1（正义裁决者与四档定向研发）

- 按 2026-09-18 三张手写稿实现 0.5.1 功能线：
  - 新增公开 E.G.O. 套装 **正义裁决者**：四件防具统一抗性 `x=0.25`；按件数 `y` 获得最高生命、
    攻速、攻击力提升，四件解锁被动「审判」（60 秒冷却，被近战攻击时对攻击者造成 15 点蓝伤）
  - 正义裁决者武器：铁剑外观、`0.4` 攻速、满蓄力攻击前方 6 格与左右各 1 格的蓝伤区域，伤害 5~6
  - 护甲与武器分开研发；设计稿基准价分别为 400 / 450
  - 蓝图改为**一次性装载物**，装载后消耗 1 张并把研发能力绑定玩家
  - 定向研发改为四档：普通 25% / 75%x、进阶 50% / 100%x、高级 75% / 150%x、完全 100% / 200%x
  - 开发台重做主界面与定向研发页面，列出护甲/武器项目，并在同一页提供四档按钮
- 新增通用能力：
  - `CustomItem` 支持配置化物品属性与盔甲纹饰
  - `EgoSetBonusService` 按当前 `y` 动态同步套装属性
  - `EgoAbilityDefinition` 与 `EgoCombatListener` 支持配置化武器技能
  - 负抗性按正倍率易伤处理，避免出现负伤害反向治疗
- 增加一份隐藏彩蛋 E.G.O. 套装，按用户要求**不写入 README 与 Release Notes**；
  实现内容包括 OP 穿戴要求、服主保护命令拦截与特殊武器
- 测试：`mvn clean test package` 通过（42 个生产源文件、8/8 单元测试）；构建产物 `Cogito-0.5.1.jar`

## 2026-09-19 · 0.5.2（正义裁决者战斗修复）

- 从 GitHub 同步最新 `main` 与 `v0.5.1`，在正式服实测物品 NBT 后修复：
  - 正义裁决者四件防具与武器统一显示名称为 **正义裁决者**，移除头盔 / 胸甲 / 护腿 / 战靴 / 裁决之剑后缀
  - 蓝伤改为 Project Moon PALE 规则：配置值表示目标最大生命值百分比，`5` 即 `5%` 最大生命值
  - 正义裁决者满蓄力挥剑即触发，不再要求必须命中实体；范围内每个目标随机结算 `8~10` 段、每段 `5%~6%` 蓝伤
  - 修正套装攻速阈值重复叠加：`y>=3` 只额外提供一次 `+0.5` 攻速
  - 登录 / 热重载时刷新玩家已有的 Cogito 物品，旧版正义裁决者无需重新发放即可应用新名称与 NBT
- 版本 → **0.5.2**

## 2026-09-19 · 0.5.3（挥剑触发与服主名称修复）

- 正式服继续修复：
  - `PlayerInteractEvent` 在部分客户端操作下无法稳定捕获挥剑，改为 `PlayerAnimationEvent.ARM_SWING`；
    命中实体仍由 `EntityDamageByEntityEvent` 兜底，空中挥剑也能触发正义裁决者范围伤害
- 服主隐藏套装四件防具与武器显示名称全部统一为 **服主**，移除“之冠 / 之甲 / 之护腿 / 之靴 / 之剑”后缀
- 服主剑左键击杀改用手臂挥动事件触发，解决 0 攻击伤害导致实体受击事件不触发的问题
- 定向研发列表在未装载蓝图时仍显示装备项目，未解锁状态只做点击拦截
- 正义裁决者四件防具统一改为铁套材质 + 钻石海岸纹饰
- 版本 → **0.5.3**

## 2026-09-19 · 0.5.4（正义裁决者单段与服主护甲外观）

- 按用户继续简化正义裁决者武器：
  - 不再做多段和区域攻击，改为单目标单段固定 `5~6` 点蓝伤
  - 攻击速度改为 `4.0`，目标距离改为 `6` 格
  - 保留 E.G.O. 蓝伤来源校验，物品 NBT 重新生成后生效
- 服主套装护甲外观改为：钻石头盔 / 胸甲 / 护腿 / 靴子 + `spire`（末地城）盔甲纹饰 + 金锭纹饰材质。
- 版本 → **0.5.4**

## 2026-09-20 · 0.5.5（金枝双端资源包）

- 为金枝制作 Java + Bedrock/Geyser 双端资源包：
  - Java 使用 `DEAD_BUSH` + `item_model: cogito:golden_bough`
  - Bedrock 使用 `cogito:golden_bough` 自定义物品纹理与 Geyser v2 `custom_mappings`
  - 资源包发布到公开仓库 `lzqkotony/download` 的 `v1.0.0` Release
- 下载地址使用 `https://v4.gh-proxy.com/https://github.com/...` 代理，照顾国内网络。
- Cogito 插件新增 `item-model` 配置支持；旧 STICK 金枝登录/热重载时自动刷新为枯木模型，不需要重新发放。
- 版本 → **0.5.5**

## 2026-09-20 · 0.5.6（正义裁决者灵魂攻击机制）

- 按用户补充的 ALEPH 武器参数实现：
  - 研发武器基准 `x=450`，装备条件不额外限制
  - 普通攻击对目标进行 5 次固定灵魂伤害，伤害翻倍为每段 `4~8`
  - 40% 概率触发特殊攻击：3 次重击 `14~20` + 7 次轻击 `4~8`
  - 攻击距离 4 格，硬冷却 40 tick（2 秒）
  - 武器 Lore 只保留数值属性、原始消耗/可研发数量信息和描述：
    `它的天平能够绝对公正地衡量任何罪恶。`
- 版本 → **0.5.6**

## 2026-09-20 · 0.5.7（正义裁决者多段时间轴与范围修正）

- 将正义裁决者从“同一 tick 打出全部段数”改为真正的 2 秒时间轴：
  - 普通攻击每 10 tick 结算 1 段，共 5 段，落在 2 秒内
  - 特殊攻击每 4 tick 结算 1 段，共 10 段
  - 每次脉冲只处理一次区域查询，最多 10 次，不注册永久 tick 任务
- 范围改为攻击者朝向的前方 `4` 格、横向宽度 `1` 格、高度 `4` 格。
- 范围内的所有 `LivingEntity` 都会受到多段伤害；服务端服主保护玩家除外。
- 修复多段伤害触发 `EntityDamageByEntityEvent` 导致递归/取消的问题，多段结算期间使用能力抑制标记。
- 版本 → **0.5.7**

## 2026-09-20 · 0.5.8（负抗性反转治疗）

- 修正负抗性语义：不再把负 x 当成易伤倍率，而是保留统一公式的负倍率。
  - `x=-10, y=4` → `r=-10`
  - 受击时取消原版伤害，并将 `-最终伤害` 等量转为治疗，最多恢复到最大生命值
  - 部分穿戴时也按相同公式回血，例如 `x=-10, y=2` → `r=-4.5`
- 版本 → **0.5.8**

---

## 开发与交付方式

### 版本号约定（2026-09-17 用户定）

- **0.5.x 是同一个功能线**：开发台、图纸解锁以及后续的修复 / 小功能（E.G.O. 代价、共鸣、饰品四部位等）
  都按**补丁号**递增 —— 已发布 `0.5.1`、`0.5.2`，后续为 `0.5.3`、`0.5.4`……
- 只有**开启新功能线**时才升 minor（例如异想体镇压产出 → `0.6.0`）。
- 每次发版仍然走 `tools/ship.ps1` / `tools/ship.sh`（导出聊天记录 → 构建 → 推送 → 打 tag → Release）。

| 场景 | 做法 |
| --- | --- |
| 本机（Windows） | `dev\build.ps1` 打包（`-DeployTo <路径>` 可直接拷到服务器 plugins） |
| 本机（Arch WSL） | 仓库在 `~/cogito-plugins`，JDK21 + Maven + Node + gh 已就绪，构建 `mvn clean package`，发布 `bash tools/ship.sh`（见 `docs/dev-setup.md`） |
| 其他机器（Linux/macOS） | `mvn clean package` 或 `bash build.sh` |
| 交付 | 把 jar 放进服务器 `plugins/`，重启或 `/reload confirm` |
| 服务器前置 | **Vault + 一个经济插件**（EssentialsX / CMI 等） |
| 用户正式服 | Rocky Linux、纯 IPv6、`mc.ezmscc.dpdns.org` |
| 聊天记录导出 | `tools/export-transcript.js`（默认读 `~/.codex/sessions/…/rollout-*.jsonl`） |

---

## 用户已拍板的设计决策

- 经济对接 **Vault**；脑啡肽是**实体物品**，不是余额数字
- 兑换**单向**：钱 → 脑啡肽，不提供反向
- 镇压异想体产出脑啡肽的区间：**ALEPH 100~60 / WAW 60~30 / HE 30~20 / TETH 20~10 / ZAYIN 10~8**
- E.G.O 定向开发：某个异想体累计**镇压 5 次**解锁；2026-09-18 新稿已改为**四档成功率/费用**（见 0.5.1 小节）
- 蓝图：**一次性装载**，装载后消耗并与玩家绑定（2026-09-18 拍板，覆盖 0.5.0 的“图纸不消耗”）
- 随机开发（抽卡）与提取探索：**后续再做**
- 注册物品必须打 NBT 标签、不保留原型功能（不可放置/合成）、脑啡肽与 Cogito 可堆叠
- **脑啡肽材质 = 绿宝石、脑啡肽模块 = 绿宝石块**（2026-09-16 确认，按第二批设计稿）
- 异想体与 E.G.O 的具体内容：后续再加

## 待拍板（详见 `design.md` 第 12 节）

1. ~~脑啡肽材质~~ ✅ 已确认：用新的（绿宝石 / 模块绿宝石块）
2. 抗性公式 `1-(1-x)y/4` 里 y 是件数还是权重、x 的上限
3. 工具型异想体的「代价」
4. E.G.O 与原版混搭：禁止穿 or 允许穿但特性失效
5. `/cogito set <rule>` 的 rule 清单
6. 「提取」与「镇压」的区别（是否消耗脑啡肽模块）

> 2~6 用户 2026-09-16 表示"先不管"，等后面需要时再定。

---

## 固定工作流（2026-09-16 起）

用户要求：**每轮工作结束都要提交源码 + 聊天记录，并按版本发 Release**。为此提供了 `tools/ship.ps1`（Windows 一键流程）：

```
1. 导出本轮聊天记录到 chat/（可用 -Since 只导增量）
2. 改 pom.xml 版本号 → mvn package 构建
3. git add/commit/push 到 main
4. 打 tag v<版本> 并推送
5. gh release create 发 Release（jar + sha256 + 说明）
```

用 `-DryRun` 可以只看流程不执行。

## 2026-09-20 · 0.5.9（失乐园套装与使徒召唤）

- 按手写稿实现 ALEPH「失乐园」护甲与铁锄武器。
- 护甲改为铁套 + 红石 `wayfinder` 纹饰，名称统一为「失乐园」，加入完整手写描述。
- 套装按件数提供最大生命、生命恢复、抗性提升、抗火和水下呼吸。
- 四件套新增实际伤害 `≤5` 免伤，以及每 10 秒增长 1 点、上限 20 点的神圣黄盾。
- 修正 E.G.O. 伤害顺序：先让 Paper 结算原版保护，再乘 `x=0.1`，最后才结算黄盾。
- 失乐园武器支持 16 格攻击、`22~28` 蓝伤、命中回复 `2~4`、减速 60% 持续 0.5 秒。
- 每 20 秒发动一次前方 `8 × 3 × 8` 特殊攻击，造成 `50~60` 蓝伤并显示黑色痕迹。
- 右键主动技能消耗 25% 最大生命，玩家飞到约 6 格高，在 6 秒内召唤 12 位使徒。
- 使徒为 66 生命、6 攻击、铁矛铁甲，距离超过 10 格时瞬突，落地 26 秒后无掉落消失。
- 召唤期间显示两双白色翅膀粒子，并向附近其他玩家发送白色 Title、缓慢与失明。
- 版本 → **0.5.9**

## 2026-09-20 · 0.5.10（神圣黄盾显示精度热修）

- 0.5.9 发布后复查发现，原版 `ABSORPTION` 药水效果最低会按 4 点吸收值显示，无法准确呈现每 10 秒增长的 1 点黄盾。
- 改用 `Damageable#setAbsorptionAmount` 直接同步吸收值，保留半颗心精度并使最高 20 点正确显示为 10 颗心。
- 黄盾的扣盾/破盾不穿透规则保持不变。
- 版本 → **0.5.10**

## 2026-09-20 · 0.5.11（失乐园免伤顺序与 6 秒悬空）

- 按测试反馈修正四件套免伤：改为先乘 `x=0.1`，最终伤害严格 `<5` 才取消。
- 明确伤害值不做下限截断，继续保留 `double` 精度。
- 主动技能开始后，施法者保持 6 秒无重力悬空，12 位使徒同步悬空，暂停寻敌和瞬突。
- 6 秒结束后恢复施法者与使徒重力，使徒落地后才开始 26 秒存活计时。
- 主动技能向施法者聊天栏发送「起来吧，我的仆从，欢唱着迎接我的到来。」；附近玩家同时收到该聊天提示。
- 版本 → **0.5.11**

### 0.5.11 补充：服主 E.G.O. 获取白名单

- `server-owner` 新增 `acquisition-whitelist`，当前允许 `lzqkotony` 与 `BE_lzqkotony`。
- 白名单支持玩家名、BE_ 名称与 UUID，匹配不区分大小写。
- `/cogito give`、整套 E.G.O. 发放、研发产出统一拒绝非白名单目标，OP 不绕过。
- 新增拾取与容器交互拦截；非白名单玩家登录时自动清理背包、盔甲、副手和末影箱中的服主物品。

### 0.5.11 补充：空挥与失乐园区块范围

- 失乐园普通攻击改为遍历玩家当前区块的全部实体，对区块内有效生物统一造成 `22~28` 蓝伤。
- 失乐园空挥不再直接返回，攻击冷却就绪时同样执行区块范围攻击。
- 保留每 20 秒优先发动的前方 `8 × 3 × 8` 特殊横扫。
- 正义裁决者增加左键空挥事件兜底；即使手臂挥动事件未触发，也会发动范围时间轴攻击。

## 2026-09-21 · 0.5.12（失乐园范围与友伤修正）

- 失乐园普通攻击从“当前区块”改为以玩家为中心的前后左右上下各 8 格立方体范围。
- 普通攻击与特殊攻击统一排除施法者和失乐园使徒，解决武器击杀自己的召唤物问题。
- 给使徒增加召唤者保护：不选召唤者为战斗目标，也不允许对召唤者结算伤害，避免被打后反击主人。
- 右键技能改用低优先级直接右键检测，并拒绝 `useItemInHand` / `useInteractedBlock`，避免铁锄锄地。
- 版本 → **0.5.12**

## 2026-09-21 · 0.5.13（失乐园免伤提前到结算前）

- 按用户确认改为原著式判定：单次原始伤害 `≤5` 直接免疫。
- 原始伤害超过 5 后才进入原版抗性、失乐园 `x=0.1` 和神圣黄盾结算。
- 修复此前“最终伤害 <5”等同于“原始伤害 <83.33”的超模判定。
- 更新物品 Lore 和相关测试。
- 版本 → **0.5.13**

## 2026-09-21 · 0.5.14（允许鞘翅功能性混搭）

- 胸甲槽允许放置普通鞘翅，不再让整套 E.G.O. 抗性失效。
- 鞘翅不计入 E.G.O. 防具件数，三个防具部件 + 鞘翅按 `y=3` 生效。
- 第四件套专属能力仍必须穿着同套 E.G.O. 胸甲才能获得。
- 其他原版防具/异套 E.G.O. 混搭失效规则保持不变。
- 版本 → **0.5.14**

### 0.5.14 补充：黄盾 ActionBar

- 为失乐园神圣黄盾增加精确数值 ActionBar，显示格式如 `神圣黄盾 13.5/20`。
- 黄盾数值变化时立即刷新；满值无变化时每 5 秒刷新一次。
- 破盾时显示 `神圣黄盾 已破碎`。

## 2026-09-22 · 0.5.15（黄盾首次充能与状态校验）

- 排查同版本多次替换可能被 Paper remap 缓存复用的问题，版本提升到 0.5.15。
- 黄盾首次初始化时立即提供 1 点，ActionBar 可马上显示 `神圣黄盾 1/20`。
- 每次黄盾扣减前重新检查当前套装是否仍为 Y=4；脱下或混搭后立即清除，避免旧盾残留。
- 明确原始伤害 `≤5` 会在黄盾之前免疫，因此不会扣盾。
- 版本 → **0.5.15**

## 2026-09-22 · 0.5.16（删除失乐园单次伤害免疫）

- 按用户要求彻底删除失乐园单次伤害免伤机制。
- 移除配置字段、代码判定、Lore 描述和相关旧测试。
- 所有伤害统一进入原版抗性、统一抗性 `x=0.1`、神圣黄盾的完整结算流程。
- 黄盾首次充能、Y=4 校验和 ActionBar 显示保持不变。
- 版本 → **0.5.16**
